<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/gramolares"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/gramolares"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gramola"
        tools:targetApi="31">
        <activity
            android:name=".Controladores.Inicio"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.gramola.Controladores.Registro">
        </activity>
        <activity
            android:name=".Controladores.Lista"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize">

        </activity>
    </application>

</manifest>